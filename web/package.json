{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "_comment:opennext": "@opennextjs/cloudflare is pinned exactly, not ranged. 1.20.6 narrowed its peer to next '>=15.5.24 <16 || >=16.3.3', and this app is on next 16.2.6 — inside that gap — so a ranged '^1.19.9' re-resolves to a version whose peer check fails and `npm install` dies with ERESOLVE. Unpin only together with moving next to >=16.3.3.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "opennextjs-cloudflare build && opennextjs-cloudflare deploy",
    "upload": "opennextjs-cloudflare build && opennextjs-cloudflare upload",
    "preview": "opennextjs-cloudflare build && opennextjs-cloudflare preview",
    "cf-typegen": "wrangler types --env-interface CloudflareEnv ./cloudflare-env.d.ts"
  },
  "dependencies": {
    "@opennextjs/cloudflare": "1.20.1",
    "next": "16.2.6",
    "openapi-fetch": "^0.13.4",
    "react": "^19.1.7",
    "react-dom": "^19.1.7"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20.19.43",
    "@types/react": "^19.1.7",
    "@types/react-dom": "^19.1.7",
    "eslint": "^9",
    "eslint-config-next": "16.2.6",
    "tailwindcss": "^4",
    "typescript": "^5.7.4",
    "wrangler": "^4.112.0"
  }
}
